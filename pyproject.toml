[project]
name = "marketing-agent"
version = "1.0.0"
description = "KI-Marketing-Agent mit Google ADK und Gradio UI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-adk",
    "gradio",
    "pandas",
    "python-dotenv",
    "google-genai",
]
